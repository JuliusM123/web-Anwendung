/**
 * @interface Titel
 * @description
 * Definiert die Struktur für den Titel eines Buches.
 * Ein Titel besteht aus einem Haupttitel und optional einem Untertitel.
 */
export interface Titel {
    titel: string;
    untertitel?: string;
}
